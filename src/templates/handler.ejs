<% // File Header %>
// Auto-generated by Invoker Generator
// Handler class for <%= className %>

<% if (typesToImport.length > 0) { %>
<% typesToImport.forEach(type => { %>
import { <%= type %> } from "../schema/<%= type.toLowerCase() %>";
<% }); %>
<% } %>
import { BaseHandler, Request, Response } from "./handler.base";

export class <%= className %> extends BaseHandler {

<% errorOverrides.forEach(errorMethod => { %>
  <%- errorMethod %>
<% }); %>

<% methodImplementations.forEach(methodImpl => { %>
  <%- methodImpl %>

<% }); %>
}